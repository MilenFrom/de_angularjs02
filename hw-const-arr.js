"use strict"

// Homework, const with array
const arr = ['Apple','Blackberry','Berry'];

console.log(arr);

arr[2] = 'Not a Berry';
console.log(arr);

// arr = ['New Berry','One more Berry', 'Another Berry in the family'];
// Behaves almost the same way as an object, modifying a key is possible, however assigning a new keys to the array is not
//console.log(arr);